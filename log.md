- [2026-07-27] Public snapshot retention: Snapshot publication must atomically replace `manifest.json` before cleanup, preserve the manifest-selected file, and prune only exact lowercase 16-character hash filenames. When changing snapshot writes, keep cleanup non-fatal and retain a bounded rollback set.
- [2026-07-27] Legacy V1 image retirement: The live public snapshot has no legacy `daylilydatabase.org` images, and V1 AHS image URLs are deprecated. Keep legacy cultivar metadata, but do not select `AhsListing.ahsImageUrl`; preserve only supported V2 and R2 image paths.
- [2026-08-05] Next.js workspace codemod scope: Running the Next.js 16.3 upgrade codemod from this npm workspace root can add framework dependencies to the root package and add `instant = false` to `packages/app/components/layout.tsx`, even though the app uses the Pages Router and does not enable Cache Components. When upgrading this repo, inspect the codemod diff, keep framework dependencies in `packages/app`, and remove Cache Components opt-outs unless that feature is explicitly enabled.
- [2026-08-20] Cart removal dialogs: Native confirmation dialogs can prevent customers from removing the last unit or emptying the cart. When changing cart controls, keep removal immediate and run `packages/app/tests/cart.spec.ts` against a mobile viewport.
- [2026-08-24] Rebloom search field: Rolling Oaks V2 cultivar data stores rebloom as a dedicated integer flag that was omitted from the public snapshot display model. Carry this flag into the snapshot and expose it as an independent Boolean filter so customers can combine rebloom with the initial bloom season.
