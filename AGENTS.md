- [2026-04-08] packages/app Turso V2 schema: `packages/app/.env` points the app at a live Turso DB that already contains `CultivarReference` and `V2AhsCultivar` alongside legacy `AhsListing`. When changing `packages/app/prisma/schema.sqlite.prisma` or public cultivar reads, source `packages/app/.env` and inspect the live DB shape first instead of assuming the checked-in schema is current.
- [2026-04-09] packages/app Prisma dev reload: After regenerating the SQLite Prisma client, restart any already-running `packages/app` `next dev` process before browser verification. Otherwise Next can keep a stale client in memory and throw runtime errors like `Unknown field cultivarReference`.
- [2026-04-09] packages/app Netlify image runtime: This app’s Netlify previews can rewrite `/_next/image` to `/_ipx`, and that runtime can fail with `IPX Error` if `sharp` is unavailable. When preview images break with `libvips-cpp.so.42` or similar `sharp` errors, inspect one failing `/_next/image` URL directly first; `images.unoptimized = true` in `packages/app/next.config.js` is only a short-term fallback if a supported Next runtime upgrade is not feasible.
- [2026-04-09] packages/app Netlify Next support floor: This repo shipped on Next `13.2.3`, but Netlify’s current Next adapter/runtime supports Next `13.5+`. If optimized images fail on deploy previews with `/_next/image` -> `/_ipx` sharp/libvips errors, prefer upgrading `next` and `eslint-config-next` to at least `13.5.x` before falling back to `images.unoptimized`.
- [2026-04-09] packages/app Netlify Forms on Next runtime: `@netlify/plugin-nextjs@5` rejects `data-netlify` inside React-rendered pages unless you also provide a static detection file. Keep the real React forms posting via JS to `packages/app/public/__forms.html`, and keep the deploy-time form definitions in that static file instead of on the Next pages.
- [2026-04-29] packages/app T3 Env setup: `@t3-oss/env-nextjs` requires TypeScript 5 and `moduleResolution: "bundler"` for package exports, and standalone Next output should transpile `@t3-oss/env-nextjs` plus `@t3-oss/env-core`. When adding env validation, keep those settings or builds/type checks can fail before app code runs.
- [2026-04-28] packages/app VPS form email: Contact and cart forms now submit to `packages/app/pages/api/forms.ts` instead of Netlify Forms and require `SMTP_USER`/`SMTP_PASS` at runtime. `from` is always `SMTP_USER`; `CONTACT_TO_EMAIL` and `CONTACT_BCC_EMAIL` accept comma-separated recipients. When moving the site off Netlify or debugging form delivery, configure Gmail SMTP env vars on the server and inspect `/api/forms` instead of `packages/app/public/__forms.html`.
- [2026-04-28] Docker build without Turso secrets: `packages/app` uses Next standalone output for VPS images, and DB-backed catalog/list routes should not run Turso reads during `next build`. When changing `getStaticPaths`, `getStaticProps`, or deploy Dockerfiles, keep Docker image builds independent of `.env`; use empty blocking paths or server-side fetching so secrets stay runtime-only.
- [2026-04-29] Browser screenshot fallback: The Browser Use in-app browser can load and inspect local pages, but `Page.captureScreenshot` may time out even on `about:blank`. When visual verification needs screenshots, keep the site open in the in-app browser for inspection and use headless Chrome CLI with `--headless=new --timeout=10000 --screenshot=...` as the screenshot fallback.
