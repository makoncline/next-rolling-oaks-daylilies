{
  "name": "next-rolling-oaks-daylilies",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w @packages/app",
    "build": "npm run build -w @packages/app",
    "start": "npm run start -w @packages/app"
  },
  "workspaces": [
    "packages/app"
  ],
  "dependencies": {
    "@libsql/client": "^0.8.1",
    "@prisma/adapter-libsql": "^6.5.0",
    "@prisma/client": "^6.5.0",
    "prisma": "^6.5.0"
  }
}
